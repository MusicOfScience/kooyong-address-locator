# Adding a New Election

1. Copy `backend/config/contest_config.yaml` and set contest metadata.
2. Add input files in `data/sample/` or another directory.
3. Ensure party labels map to colours in config.
4. Run `streamlit run frontend/app.py` and verify tabs render.
